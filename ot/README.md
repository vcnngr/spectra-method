# SPECTRA OT/ICS Security Kit (`ot`)

Generated by `spectra-new-module`.

## Contents

- `agents/spectra-agent-ot-ics/` — module agent
- `workflows/spectra-ot-assessment/` — module workflow
- `module.yaml`, `config.yaml` — module metadata and configuration

## Registering this module

Add the agent and workflow rows printed by the scaffold to `_config/agent-manifest.csv` and `_config/skill-manifest.csv`, then run `npm run build:skill-index` and `npm run validate`.
